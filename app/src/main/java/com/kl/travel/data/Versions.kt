package com.kl.travel.data

/** Version of the Google Sheet template bundled in assets/KLTravel_Template.xlsx. Bump it with every template change (and CHANGELOG.md). */
object Versions {
    const val TEMPLATE = "2.7"
}
